{
  "checks": {
    "app-website": {
      "command": "/usr/lib/nagios/plugins/check_http --string=Hello --hostname=localhost",
      "subscribers": [ "app" ],
      "interval": 3,
      "handler": "st2"
    }
  }
}
